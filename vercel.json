{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && NODE_ENV=production next build",
  "devCommand": "unset DATABASE_URL && next dev -p 3000",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}